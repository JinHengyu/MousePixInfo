// global['magnifier'] = document.querySelector('magnifier')
// global['canvas'] = document.querySelector('#magnifier canvas')

// let array = robot.captureScreen(0, 0, 10, 10).image
// let base64Data = btoa(String.fromCharCode.apply(null, array));

// let image = document.createElement('img')

// image.src = 'data:image/jpg;base64,' + base64Data;
// document.body.innerHTML=''
// document.body.appendChild(image)

// // canvas.getContext('2d').drawImage(, 0, 0)